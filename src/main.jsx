import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../App.jsx'
import '../App.css'

// Temporarily disable StrictMode to avoid AsyncMode errors
ReactDOM.createRoot(document.getElementById('root')).render(
  <App />
)
